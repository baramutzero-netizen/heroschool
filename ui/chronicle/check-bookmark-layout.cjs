const {chromium}=require('playwright'),assert=require('assert');
(async()=>{const b=await chromium.launch({headless:true,channel:'msedge'}),p=await b.newPage({viewport:{width:1600,height:1500}});await p.goto('http://127.0.0.1:18765/ui/chronicle/preview.html');await p.waitForFunction(()=>document.querySelector('iframe').contentDocument?.querySelector('.chronicle'));const f=p.frames()[1];await f.evaluate(()=>document.fonts.ready);await p.waitForTimeout(1100);const measure=()=>f.evaluate(()=>['.chronicle','.chron-masthead','.chron-focus-heading','.chron-days','#btnWeek'].map(s=>{const r=document.querySelector(s).getBoundingClientRect();return [r.x,r.y,r.width,r.height]}));const before=await measure();const css=await f.addStyleTag({content:'.chron-bookmark{height:700px!important;min-height:700px!important}.chron-bookmark-stage{--hand-top:100px;--hand-left:-170px}'});assert.deepEqual(await measure(),before);await css.evaluate(e=>e.remove());await f.addStyleTag({content:'.topbar{position:static!important}'});await p.screenshot({path:'ui/chronicle/bookmarks-position.png',fullPage:true});console.log('PASS: changing bookmark size and position leaves book, headers, schedule and proceed-button bounds identical');await b.close()})();
